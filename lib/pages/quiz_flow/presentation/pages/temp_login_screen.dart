import 'package:flutter/material.dart';

class LoinPage extends StatelessWidget {
  const LoinPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('temp login page'),
    );
  }
}
